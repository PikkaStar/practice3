<% unless current_user.id == user.id %>
      <% if current_user.following?(user) %>
        <%= link_to "フォロー解除",user_relationships_path(user.id),remote: true,method: :delete,class: "btn btn-danger" %>
        <% else %>
        <%= link_to "フォロー",user_relationships_path(user.id),remote: true,method: :post,class: "btn btn-success" %>
      <% end %>
      <% end %>